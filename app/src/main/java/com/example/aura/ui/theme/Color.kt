package com.example.aura.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Aura Dark Theme Palette ─────────────────────────────────
// Fashion-forward dark mode inspired by SSENSE / Zara aesthetics

/** Primary background — deep black */
val AuraBlack = Color(0xFF0A0A0A)

/** Surface color — slightly lighter for cards/sheets */
val AuraSurface = Color(0xFF161616)

/** Surface variant — subtle contrast for chat bubbles */
val AuraSurfaceVariant = Color(0xFF222222)

/** Primary accent — warm gold */
val AuraPrimary = Color(0xFFE8C547)

/** On-primary — dark text on gold backgrounds */
val AuraOnPrimary = Color(0xFF1A1200)

/** Secondary — soft purple for complementary accents */
val AuraSecondary = Color(0xFFA78BFA)

/** Tertiary — subtle teal for tags/chips */
val AuraTertiary = Color(0xFF2DD4BF)
val AuraTertiaryContainer = Color(0xFF1A3330)
val AuraOnTertiaryContainer = Color(0xFF2DD4BF)

/** Text colors */
val AuraTextPrimary = Color(0xFFF5F5F5)
val AuraTextSecondary = Color(0xFF9CA3AF)

/** Secondary container — for recommendation cards */
val AuraSecondaryContainer = Color(0xFF1E1B2E)
val AuraOnSecondaryContainer = Color(0xFFE0D9F5)

/** Outline — subtle borders */
val AuraOutline = Color(0xFF333333)

/** Error */
val AuraError = Color(0xFFEF4444)

// ─── Light Theme (fallback, not primary) ────────────────────
val AuraLightBackground = Color(0xFFFAFAFA)
val AuraLightSurface = Color(0xFFFFFFFF)
val AuraLightPrimary = Color(0xFFB8960F)
val AuraLightOnPrimary = Color(0xFFFFFFFF)